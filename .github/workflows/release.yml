name: Release

# The version comes from project.yml; the run name carries the channel and unique run number.
run-name: "Spotter · ${{ inputs.channel }} #${{ github.run_number }}"

# Manually triggered from the Actions tab. Pick a channel;
# beta builds get an auto-incrementing suffix so re-running never collides.
on:
  workflow_dispatch:
    inputs:
      channel:
        description: "Release channel"
        type: choice
        options: [beta, stable]
        default: beta

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v7

      - name: Select Xcode 26
        run: |
          XCODE_PATH="$(ls -d /Applications/Xcode_26*.app 2>/dev/null | sort -V | tail -n1)"
          if [ -z "$XCODE_PATH" ]; then
            echo "::error::No Xcode 26.x found on this runner. Installed Xcodes:"
            ls /Applications | grep -i xcode || true
            exit 1
          fi
          echo "Using $XCODE_PATH"
          echo "DEVELOPER_DIR=$XCODE_PATH/Contents/Developer" >> "$GITHUB_ENV"

      - name: Compute release metadata
        id: meta
        run: |
          CHANNEL="${{ inputs.channel }}"
          BASE_VERSION="$(sed -nE 's/^[[:space:]]*MARKETING_VERSION:[[:space:]]*"([^"]+)".*/\1/p' project.yml)"
          VERSION_COUNT="$(printf '%s\n' "$BASE_VERSION" | sed '/^$/d' | wc -l | tr -d ' ')"
          if [ "$VERSION_COUNT" -ne 1 ] || [[ ! "$BASE_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::project.yml must contain exactly one numeric MARKETING_VERSION (x.y.z)."
            exit 1
          fi
          if [ "$CHANNEL" = "stable" ] && [ "$GITHUB_REF_NAME" != "${{ github.event.repository.default_branch }}" ]; then
            echo "::error::Stable releases must run from the default branch."
            exit 1
          fi
          # Each channel is a distinct, side-by-side app: its own display name + bundle id,
          # and its own Homebrew cask in the mmmmmmarcus/homebrew-spotter tap.
          case "$CHANNEL" in
            beta)   SUFFIX="-beta.${GITHUB_RUN_NUMBER}";  DISPLAY="Spotter Beta";  BUNDLE="com.spotter.app.beta";  CASK="spotter@beta";  PRERELEASE=true ;;
            stable) SUFFIX="";                            DISPLAY="Spotter";       BUNDLE="com.spotter.app";       CASK="spotter";       PRERELEASE=false ;;
          esac
          FULL="${BASE_VERSION}${SUFFIX}"
          if git ls-remote --exit-code --tags origin "refs/tags/v${FULL}" >/dev/null 2>&1; then
            echo "::error::Release tag v${FULL} already exists."
            exit 1
          fi
          {
            echo "full_version=${FULL}"
            echo "display_name=${DISPLAY}"
            echo "bundle_id=${BUNDLE}"
            echo "cask_name=${CASK}"
            echo "dmg_file=Spotter-${FULL}.dmg"
            echo "prerelease=${PRERELEASE}"
          } >> "$GITHUB_OUTPUT"

      # Import only the Developer ID identity into an ephemeral keychain. See docs/signing.md.
      - name: Import Developer ID certificate
        env:
          P12_BASE64: ${{ secrets.DEVELOPER_ID_P12_BASE64 }}
          P12_PASSWORD: ${{ secrets.DEVELOPER_ID_P12_PASSWORD }}
        run: |
          if [ -z "${P12_BASE64}" ] || [ -z "${P12_PASSWORD}" ]; then
            echo "::error::Developer ID signing secrets are not configured. See docs/signing.md."
            exit 1
          fi
          KEYCHAIN="$RUNNER_TEMP/signing.keychain-db"
          KPW="$(openssl rand -base64 24)"
          echo "${P12_BASE64}" | base64 --decode > "$RUNNER_TEMP/cert.p12"
          security create-keychain -p "$KPW" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$KPW" "$KEYCHAIN"
          security import "$RUNNER_TEMP/cert.p12" -k "$KEYCHAIN" -P "${P12_PASSWORD}" -A -T /usr/bin/codesign
          # Let codesign use the key without an interactive prompt.
          security set-key-partition-list -S apple-tool:,apple: -s -k "$KPW" "$KEYCHAIN" >/dev/null
          # Prepend to the search list so xcodebuild finds the imported Developer ID identity.
          security list-keychains -d user -s "$KEYCHAIN" $(security list-keychains -d user | sed 's/"//g')
          rm -f "$RUNNER_TEMP/cert.p12"

      - name: Validate notarization credentials
        env:
          APPLE_ID: ${{ secrets.APPLE_NOTARY_APPLE_ID }}
          APP_PASSWORD: ${{ secrets.APPLE_NOTARY_PASSWORD }}
        run: |
          if [ -z "${APPLE_ID}" ] || [ -z "${APP_PASSWORD}" ]; then
            echo "::error::Apple notarization secrets are not configured. See docs/signing.md."
            exit 1
          fi

      # Build the signed .app into an ephemeral DerivedData dir. Channel identity (name, bundle id,
      # version) comes from build-setting overrides so beta/stable ship side-by-side.
      - name: Build app
        env:
          VERSION: ${{ steps.meta.outputs.full_version }}
          DISPLAY_NAME: ${{ steps.meta.outputs.display_name }}
          BUNDLE_ID: ${{ steps.meta.outputs.bundle_id }}
          BUILD_NUMBER: ${{ github.run_number }}
        run: |
          xcodebuild -project Spotter.xcodeproj -scheme Spotter -configuration Release \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY="Developer ID Application" \
            DEVELOPMENT_TEAM="SM96W8VVK9" ENABLE_HARDENED_RUNTIME=YES \
            OTHER_CODE_SIGN_FLAGS="--timestamp" \
            PRODUCT_NAME="$DISPLAY_NAME" PRODUCT_BUNDLE_IDENTIFIER="$BUNDLE_ID" \
            MARKETING_VERSION="$VERSION" CURRENT_PROJECT_VERSION="$BUILD_NUMBER" \
            build

      - name: Verify and notarize app
        env:
          DISPLAY_NAME: ${{ steps.meta.outputs.display_name }}
          APPLE_ID: ${{ secrets.APPLE_NOTARY_APPLE_ID }}
          APP_PASSWORD: ${{ secrets.APPLE_NOTARY_PASSWORD }}
        run: |
          APP="$RUNNER_TEMP/DerivedData/Build/Products/Release/${DISPLAY_NAME}.app"
          codesign --verify --deep --strict --verbose=2 "$APP"
          SIGNATURE_INFO="$(codesign -dv --verbose=4 "$APP" 2>&1)"
          TEAM="$(sed -n 's/^TeamIdentifier=//p' <<< "$SIGNATURE_INFO")"
          if [ "$TEAM" != "SM96W8VVK9" ]; then
            echo "::error::Expected TeamIdentifier SM96W8VVK9, found ${TEAM:-none}."
            exit 1
          fi
          if ! grep -q 'flags=.*runtime' <<< "$SIGNATURE_INFO"; then
            echo "::error::Hardened Runtime is not present in the app signature."
            exit 1
          fi
          ditto -c -k --keepParent "$APP" "$RUNNER_TEMP/app-notarization.zip"
          xcrun notarytool submit "$RUNNER_TEMP/app-notarization.zip" \
            --apple-id "$APPLE_ID" --team-id "SM96W8VVK9" --password "$APP_PASSWORD" --wait
          xcrun stapler staple "$APP"
          xcrun stapler validate "$APP"
          spctl --assess --type execute --verbose=2 "$APP"

      # Pack the stapled .app + an /Applications symlink into a signed DMG under dist/.
      - name: Package DMG
        env:
          DISPLAY_NAME: ${{ steps.meta.outputs.display_name }}
          DMG_FILE: ${{ steps.meta.outputs.dmg_file }}
        run: |
          STAGE="$(mktemp -d)"
          cp -R "$RUNNER_TEMP/DerivedData/Build/Products/Release/${DISPLAY_NAME}.app" "$STAGE/"
          ln -s /Applications "$STAGE/Applications"
          mkdir -p dist
          diskutil image create from "$STAGE" --format UDZO --volumeName "$DISPLAY_NAME" "dist/${DMG_FILE}"
          codesign --sign "Developer ID Application" --timestamp "dist/${DMG_FILE}"

      - name: Notarize DMG
        env:
          DMG_FILE: ${{ steps.meta.outputs.dmg_file }}
          APPLE_ID: ${{ secrets.APPLE_NOTARY_APPLE_ID }}
          APP_PASSWORD: ${{ secrets.APPLE_NOTARY_PASSWORD }}
        run: |
          xcrun notarytool submit "dist/${DMG_FILE}" \
            --apple-id "$APPLE_ID" --team-id "SM96W8VVK9" --password "$APP_PASSWORD" --wait
          xcrun stapler staple "dist/${DMG_FILE}"
          xcrun stapler validate "dist/${DMG_FILE}"
          spctl --assess --type open --context context:primary-signature --verbose=2 "dist/${DMG_FILE}"

      # The updater receives the same signed, notarized and stapled app as the DMG.
      - name: Package update zip
        env:
          DISPLAY_NAME: ${{ steps.meta.outputs.display_name }}
          FULL_VERSION: ${{ steps.meta.outputs.full_version }}
        run: |
          ditto -c -k --keepParent \
            "$RUNNER_TEMP/DerivedData/Build/Products/Release/${DISPLAY_NAME}.app" \
            "dist/Spotter-${FULL_VERSION}.zip"

      - uses: actions/upload-artifact@v7
        with:
          name: Spotter-${{ steps.meta.outputs.full_version }}
          path: dist/

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="v${{ steps.meta.outputs.full_version }}"
          PRERELEASE_FLAG=""
          if [ "${{ steps.meta.outputs.prerelease }}" = "true" ]; then
            PRERELEASE_FLAG="--prerelease"
          fi
          gh release create "$TAG" \
            "dist/${{ steps.meta.outputs.dmg_file }}" \
            "dist/Spotter-${{ steps.meta.outputs.full_version }}.zip" \
            --title "${{ steps.meta.outputs.display_name }} ${{ steps.meta.outputs.full_version }}" \
            --notes "**Channel:** ${{ inputs.channel }}
          **Version:** ${{ steps.meta.outputs.full_version }}
          **Bundle ID:** \`${{ steps.meta.outputs.bundle_id }}\`
          Built from ${{ github.sha }}.

          **Install via Homebrew:**
          \`\`\`sh
          brew install --cask mmmmmmarcus/spotter/${{ steps.meta.outputs.cask_name }}
          \`\`\`
          This build is signed with Developer ID and notarized by Apple, so the DMG can also be opened directly without clearing quarantine attributes." \
            $PRERELEASE_FLAG

      - name: Update Homebrew cask
        env:
          TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          CASK: ${{ steps.meta.outputs.cask_name }}
          VERSION: ${{ steps.meta.outputs.full_version }}
          DMG_FILE: ${{ steps.meta.outputs.dmg_file }}
        run: |
          if [ -z "${TAP_TOKEN}" ]; then
            echo "::warning::HOMEBREW_TAP_TOKEN secret not set — skipping cask bump. Add it to enable auto-updates of the tap."
            exit 0
          fi
          SHA="$(shasum -a 256 "dist/${DMG_FILE}" | awk '{print $1}')"
          git clone --depth 1 "https://x-access-token:${TAP_TOKEN}@github.com/mmmmmmarcus/homebrew-spotter.git" tap
          CASK_FILE="tap/Casks/${CASK}.rb"
          if [ ! -f "$CASK_FILE" ]; then
            echo "::error::$CASK_FILE not found in tap."
            exit 1
          fi
          sed -i '' -E "s|^  version \".*\"|  version \"${VERSION}\"|" "$CASK_FILE"
          sed -i '' -E "s|^  sha256 \".*\"|  sha256 \"${SHA}\"|" "$CASK_FILE"
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "Casks/${CASK}.rb"
          if git diff --cached --quiet; then
            echo "No cask changes to commit."
          else
            git commit -m "${CASK} ${VERSION}"
            git push
          fi

      # Announce the release in the Discord release channel. A Components V2 (flags 32768) card:
      # channel-tinted container, title + right-aligned Download button, Homebrew install block.
      # No-ops with a warning if the webhook secret isn't set.
      - name: Announce on Discord
        if: success()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          CHANNEL: ${{ inputs.channel }}
          DISPLAY_NAME: ${{ steps.meta.outputs.display_name }}
          VERSION: ${{ steps.meta.outputs.full_version }}
          CASK: ${{ steps.meta.outputs.cask_name }}
          TAG: v${{ steps.meta.outputs.full_version }}
        run: |
          if [ -z "${DISCORD_WEBHOOK_URL}" ]; then
            echo "::warning::DISCORD_WEBHOOK_URL secret not set — skipping Discord announcement."
            exit 0
          fi
          if [ "${CHANNEL}" = "stable" ]; then COLOR=3066993; else COLOR=15844367; fi
          RELEASE_URL="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/releases/tag/${TAG}"
          INSTALL="brew install --cask mmmmmmarcus/spotter/${CASK}"
          # Build with jq so backticks stay literal (printf in single quotes escaped them,
          # which Discord rendered as verbatim backticks instead of a code block).
          jq -n \
            --argjson color "${COLOR}" \
            --arg heading "## 🚀 ${DISPLAY_NAME} ${VERSION}" \
            --arg sub "Now available on the **${CHANNEL}** channel." \
            --arg install "${INSTALL}" \
            --arg url "${RELEASE_URL}" \
            '{
              flags: 32768,
              components: [
                { type: 17, accent_color: $color, components: [
                  { type: 9,
                    components: [ { type: 10, content: ($heading + "\n" + $sub) } ],
                    accessory: { type: 2, style: 5, label: "Download", url: $url }
                  },
                  { type: 14 },
                  { type: 10, content: ("**Install / update via Homebrew**\n```\n" + $install + "\n```") }
                ] }
              ]
            }' > payload.json
          curl -fsS -X POST -H "Content-Type: application/json" -d @payload.json \
            "${DISCORD_WEBHOOK_URL}?with_components=true"
